# Vite dev server proxy target for the Go API.
# Keep this in sync with the API service's HTTP_ADDR.
VITE_API_PROXY_TARGET=http://localhost:8080

# Browser-facing API base URL. Defaults to /api when unset.
VITE_API_BASE_URL=/api
